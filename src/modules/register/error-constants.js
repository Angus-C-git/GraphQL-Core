const emailInUse = "email already in use";
const emailMinLength = "email must be at least 3 characters";
const emailMaxLength = "email cannot exceed 255 characters";
const emailInvalid = "email must be a valid email"

const passwordMinLength = "password must be at least 8 characters"

module.exports = {
	emailInUse,
	emailMinLength,
	emailMaxLength,
	emailInvalid,
	passwordMinLength,
}